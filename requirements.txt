# Tensorflow.
tensorflow-cpu;sys_platform != 'darwin'
tensorflow;sys_platform == 'darwin'
tensorflow-text>=2.20.1;platform_system != 'Windows'

# Torch.
--extra-index-url https://download.pytorch.org/whl/cpu
litert-torch
torch>=2.12
torchvision

# Jax.
jax[cpu]

# LiteRT (for the model.export(format="litert") tests).
ai-edge-litert

# LiteRT-LM export dependencies (litert-lm-builder) live in the `litertlm`
# extra (see pyproject.toml), not here: they are only usable with the PyTorch
# backend, and keras_hub's litertlm export code already guards its usage at
# runtime via `importlib.util.find_spec`, so JAX/TF CI environments do not
# need them installed.

-r requirements-common.txt
