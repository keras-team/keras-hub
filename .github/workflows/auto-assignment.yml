name: auto-assignment

on:
  issues:
    types:
      - opened
  pull_request_target: # zizmor: ignore[dangerous-triggers]
    types:
      - opened

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  auto_assign:
    uses: keras-team/shared-workflows/.github/workflows/auto-assignment.yml@5ee0604fc2fb4bf0ff6032083a20d1da69e8b923 # v2
    with:
      ref: '5ee0604fc2fb4bf0ff6032083a20d1da69e8b923'
      issue_assignees: 'maitry63,mrinalghoshh,dhantule'
      # TODO(jeffcarp): Transition to CODEOWNERS for this.
      pr_reviewers: 'laxmareddyp,JyotinderSingh'
